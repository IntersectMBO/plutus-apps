# plutus-pab-executables tests

There is a test suite in `plutus-pab.cabal`:

* `plutus-pab-test-full` has the main PAB tests (for `Plutus.PAB.Core` and related modules). It depends on `plutus-use-cases`.
